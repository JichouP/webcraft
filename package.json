{
  "dependencies": {
    "pixi.js": "^4.6.2",
    "websocket": "^1.0.25"
  },
  "scripts": {
    "start": "parcel index.html --out-dir build",
    "watch": "parcel watch index.html --out-dir build"
  },
  "devDependencies": {
    "parcel-bundler": "^1.4.1"
  }
}
